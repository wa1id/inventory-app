{
  "name": "inventory-sync",
  "version": "1.0.0",
  "description": "Photo and backup storage for the Inventory app, backed by Cloudflare R2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.118.0"
  }
}
